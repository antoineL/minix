# Makefile for the boot monitor package.

.include <bsd.own.mk>

# various boot blocks from NetBSD, ELF-compatible
SUBDIR+= mbr cdboot

# 16-bit code; needs the i86 ACK library
SUBDIR+= boot

.include <bsd.subdir.mk>
